import React from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default function IntroRecipe() {
  return (
    <View style={styles.container}>
      <Text>
        Everyone loves to cook once in a lifetime.So Min Recipe gives access to
        those who are interested to cook a simple recipe with instrutions. Here
        you will get different varieties of recipeS .By clicking on NewRecipe
        you will get a new recipe.Then why late, lets start cooking and enjoy
        the virtual recipe.
      </Text>
    </View>
  )
}

const styles = StyleSheet.create({})
